{
  "name": "bapupura-sanskar-bhavan-cms",
  "version": "1.0.0",
  "description": "Production-ready CMS & Admin Dashboard for Shri Raghuveer Chaudhary Sanskar Bhavan",
  "main": "backend/server.js",
  "scripts": {
    "setup": "npm run install:all && npm run build:admin",
    "install:all": "npm install && npm --prefix backend install && npm --prefix admin install",
    "dev": "concurrently \"npm run dev:backend\" \"npm run dev:admin\"",
    "dev:backend": "npm --prefix backend run dev",
    "dev:admin": "npm --prefix admin run dev",
    "build:admin": "npm --prefix admin run build",
    "start": "npm --prefix backend start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
